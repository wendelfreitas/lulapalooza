import styled from 'styled-components';

export const Container = styled.div`
  background: #ffcd00;
  position: fixed;
  bottom: 3.5rem;
  padding: 2rem;
  right: 3rem;
  border-radius: 3rem;
  font-size: 1.75rem;
  font-weight: 500;
  color: #64540d;

  &:hover {
    cursor: pointer;
  }
`;
